name: Release Docker images (manual)

on:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker image release workflow is now manual-only."
